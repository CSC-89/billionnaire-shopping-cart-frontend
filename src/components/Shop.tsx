import { ShopType } from '@/types/componentTypes'
import { Billionnaire } from '@/types/dataTypes'
import React, { FC } from 'react'

const Shop: FC<ShopType> = () => {
  return (
    <div>Shop</div>
  )
}

export default Shop